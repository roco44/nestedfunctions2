
//From Educba
//Outer function() with single inner function()
function triangleHypotenuse(base, height)
{
    function square (side, side)
    {
    return side * side;
    }

    return Math.sqrt(square(base) + square(height));
}
    //document.write("The hypotenuse of this triangle is: " + triangleHypotenuse(3,4));
    //alert(triangleHypotenuse(3,4));
    console.log(triangleHypotenuse(3,4))


// function triangleHypotenuse(base,height)
// {
// function square(side){
// return side*side;
// }
// return Math.sqrt(square(base)+square(height));
// }
// document.write("Hypotenuse of triangle is :"+triangleHypotenuse(3,4));

